rules:
  - key: Obj Prop 2
    type: TRACK
    description: Obj Prop 20
    version: 1
    labels:
      feature: journeys
    properties:
      object:
        description: this is a single object des  here
        properties:
          int:
            description: this is an optional sub-property in an object hey hey
            type: number
          strings:
            description: this is a required sub-property in an object here
            type: string
        type: object
        required: true
      object2:
        description: this is a single property
        properties:
          string:
            description: this is a required sub-property in an object
            type: string
        type: object
