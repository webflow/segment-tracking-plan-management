rules:
  - key: '[Client payments] Client Invite Accepted'
    type: TRACK
    description: '[Client payments] Client Invite Accepted'
    version: 1
    labels: {}
    properties:
      site id:
        type:
          - string
      site slug:
        type:
          - string
      siteName:
        type:
          - string
      userId:
        type:
          - string
      workspace addons:
        properties:
          full seat count:
            type:
              - integer
          limited seat count:
            type:
              - integer
        type: object
      workspace id:
        type:
          - string
      workspace plan:
        type:
          - string
      workspace plan id:
        type:
          - string
      workspace plan type:
        type:
          - string
      workspace role:
        type:
          - string
      workspace seat count:
        type:
          - integer
      workspace slug:
        type:
          - string
      workspaceRole:
        type:
          - string
