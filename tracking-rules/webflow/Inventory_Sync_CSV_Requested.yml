rules:
  - key: Inventory Sync CSV Requested
    type: TRACK
    description: Inventory Sync CSV Requested
    version: 1
    labels: {}
    properties:
      inv_sync_enabled:
        type:
          - boolean
      inv_sync_platform:
        type:
          - string
      inv_sync_products_total:
        type:
          - integer
      inv_sync_variants_total:
        type:
          - integer
      source:
        type:
          - string
      wf_source:
        type:
          - string
